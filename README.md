# Telegram Video Analytics Bot

Telegram-бот для аналитики видео с использованием естественного языка (русский). Преобразует текстовые вопросы в SQL-запросы с помощью локальной LLM модели SQLCoder через Ollama.

## Технологии
- [Ollama](https://ollama.com) - локальный LLM inference
- [SQLCoder](https://github.com/defog-ai/sqlcoder) - модель для генерации SQL
- [aiogram](https://github.com/aiogram/aiogram) - Telegram Bot framework
- [SQLAlchemy](https://www.sqlalchemy.org/) - SQL toolkit для Python
- [PosgreSQL](https://www.postgresql.org/) - База данных

## Структура проекта
```
app/
├── alembic/                # Миграции БД
│   ├── env.py
│   └── versions/
├── app/                    # Основной код
│   ├── bot.py              # Telegram бот (aiogram)
│   ├── config.py           # Конфигурация из .env
│   ├── db.py               # Database layer (SQLAlchemy)
│   ├── llm_processor.py    # NL → SQL (Ollama + SQLCoder)
│   └── models.py           # ORM модели
├── scripts/
│   └── load_data.py        # Загрузка videos.json в БД
├── tests/
│   └── test_llm_processor.py  # Тесты
├── data/
│   └── videos.json        # Исходные данные
├── docker-compose.yml     # Оркестрация сервисов
├── Dockerfile             # Образ бота
├── init-db.sh             # Создание readonly пользователя
├── requirements.txt       # Python зависимости
└── .env                   # Переменные окружения
```

##  Быстрый старт

### Требования

-   Docker и Docker Compose
-   Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Запуск за 3 шага

```bash
# 1. Клонировать репозиторий
git clone <your-repo-url>
cd telegram-video-analytics-bot

# 2. Настроить переменные окружения
cp .env.example .env
# Добавить TELEGRAM_BOT_TOKEN в .env

# 3. Положить данные и запустить
mkdir -p data
cp /path/to/videos.json data/
docker-compose up -d

```

**Готово!** Бот автоматически:

-   Запустит PostgreSQL
-   Скачает модель SQLCoder (~4GB, первый запуск занимает 5-10 минут)
-   Применит миграции БД
-   Загрузит данные из `videos.json`
-   Запустит бота

##  Конфигурация

### Переменные окружения (.env)

Создайте `.env` файл на основе `.env.example`:

```bash
# Обязательные
TELEGRAM_BOT_TOKEN=your_bot_token_here

# PostgreSQL - Admin (для миграций и загрузки данных)
POSTGRES_ADMIN_USER=admin
POSTGRES_ADMIN_PASSWORD=admin_password

# PostgreSQL - Readonly (для бота - только SELECT)
POSTGRES_READONLY_USER=readonly_user
POSTGRES_READONLY_PASSWORD=readonly_password

# PostgreSQL - Общие настройки
POSTGRES_DB=video_analytics
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Ollama
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=sqlcoder:latest

```

### Получение Telegram Bot Token

1.  Открыть [@BotFather](https://t.me/BotFather) в Telegram
2.  Отправить `/newbot`
3.  Следовать инструкциям
4.  Скопировать токен в `.env`

## Docker сервисы

```yaml
services:
  postgres:      # PostgreSQL 16
  ollama:        # Ollama сервер
  ollama-setup:  # Загрузка модели SQLCoder
  migrations:    # Применение миграций (admin user)
  data-loader:   # Загрузка данных (admin user)
  bot:           # Telegram бот (readonly user)

```
## Архитектура

### Общая схема

```
┌─────────────┐
│ Telegram    │
│ User        │
└──────┬──────┘
       │ "Сколько видео набрало 100k просмотров?"
       ▼
┌──────────────────────────────────┐
│ Telegram Bot (aiogram)           │
│ - Принимает текст на русском     │
│ - Отправляет в LLM processor     │
└──────┬───────────────────────────┘
       ▼
┌──────────────────────────────────┐
│ LLM Processor                    │
│ - Переводит даты (ноября → 11)  │
│ - Формирует промпт с схемой БД   │
│ - Отправляет в Ollama            │
└──────┬───────────────────────────┘
       ▼
┌──────────────────────────────────┐
│ Ollama + SQLCoder                │
│ - Локальная LLM модель           │
│ - Специализирована на SQL        │
│ - Генерирует SQL запрос          │
└──────┬───────────────────────────┘
       ▼
┌──────────────────────────────────┐
│ SQL Validator                    │
│ - Проверка: только SELECT        │
│ - Блокирует DROP/DELETE/UPDATE  │
└──────┬───────────────────────────┘
       ▼
┌──────────────────────────────────┐
│ PostgreSQL (readonly user)       │
│ - Выполняет SELECT запрос        │
│ - Возвращает одно число          │
└──────┬───────────────────────────┘
       ▼
┌──────────────────────────────────┐
│ User получает ответ: "42"        │
└──────────────────────────────────┘

```

### Компоненты

#### 1. **Telegram Bot** (`app/bot.py`)

-   Использует aiogram 3.x (асинхронный)
-   Обрабатывает команды `/start`, `/help`
-   Принимает текстовые вопросы на русском
-   Отправляет результат пользователю

#### 2. **LLM Processor** (`app/llm_processor.py`)

-   Преобразует естественный язык → SQL
-   Использует Ollama + SQLCoder (локально, бесплатно)
-   Валидирует сгенерированный SQL

**Ключевые особенности:**

-   **Промпт-инженеринг**: Детальное описание схемы БД + примеры запросов
-   **Обработка дат**: "28 ноября 2025" → "2025-11-28"
-   **Безопасность**: Только SELECT запросы, блокировка опасных операций

**Пример промпта для SQLCoder:**

```
### Task
Generate a SQL query to answer: "Сколько видео набрало больше 100000 просмотров?"

### Database Schema
CREATE TABLE videos (
    id VARCHAR(36) PRIMARY KEY,
    creator_id VARCHAR(32) NOT NULL,
    views_count BIGINT NOT NULL,
    ...
);

### Instructions
- Return ONLY the SQL query
- The query must return a single number
- Use COALESCE for NULL handling

### Examples
Question: "How many videos got more than 100000 views?"
SQL: SELECT COUNT(*) FROM videos WHERE views_count > 100000

### SQL Query

```

**SQLCoder возвращает:**

```sql
SELECT COUNT(*) FROM videos WHERE views_count > 100000

```

#### 3. **Database Layer** (`app/db.py`)

-   SQLAlchemy 2.0 с asyncpg (асинхронный драйвер PostgreSQL)
-   **Два режима подключения:**
    -   **Admin** (для миграций и загрузки данных) - полные права
    -   **Readonly** (для бота) - только SELECT

**Безопасность на уровне БД:**

```python
# Бот использует readonly пользователя
db.init(use_admin=False)

# Даже если LLM сгенерирует DROP TABLE
# PostgreSQL откажет: "permission denied"

```

#### 4. **Models** (`app/models.py`)

-   SQLAlchemy ORM модели
-   Две таблицы: `videos` и `video_snapshots`
-   Индексы для оптимизации запросов

**Структура данных:**

```
videos:
  - id (UUID string)
  - creator_id (MD5 hash string)
  - video_created_at (timestamp)
  - views_count, likes_count, comments_count, reports_count
  
video_snapshots:
  - id (MD5 hash string)
  - video_id (ссылка на videos)
  - delta_views_count, delta_likes_count (приросты)
  - created_at (время замера)

```

## 🔐 Безопасность

### Разделение полномочий (Principle of Least Privilege)

Проект использует **два PostgreSQL пользователя**:

1.  **Admin** (`admin:admin_password`)
    
    -   Полные права на БД
    -   Используется ТОЛЬКО для:
        -   Применения миграций (`migrations` контейнер)
        -   Загрузки данных (`data-loader` контейнер)
2.  **Readonly** (`readonly_user:readonly_password`)
    
    -   Только `SELECT` на все таблицы
    -   Используется ботом для выполнения запросов
    -   **НЕ МОЖЕТ** выполнить `INSERT`, `UPDATE`, `DELETE`, `DROP`

### Многоуровневая защита

```
┌─────────────────────────────────────┐
│ Уровень 1: SQL Validator (Python)  │
│ - Блокирует не-SELECT запросы       │
│ - Проверяет опасные ключевые слова  │
└─────────────┬───────────────────────┘
              ▼
┌─────────────────────────────────────┐
│ Уровень 2: PostgreSQL permissions  │
│ - Readonly user физически не может  │
│   изменить данные                   │
└─────────────────────────────────────┘

```

**Пример:**

Если LLM ошибётся и сгенерирует:

```sql
DELETE FROM videos WHERE id = '123'

```

1.  SQL Validator откажет: "Non-SELECT query rejected"
2.  Если валидатор пропустит → PostgreSQL откажет: "permission denied for table videos"



## Примеры запросов

Бот понимает вопросы на русском:

```
"Сколько всего видео есть в системе?"
→ SELECT COUNT(*) FROM videos

"Сколько видео набрало больше 100000 просмотров?"
→ SELECT COUNT(*) FROM videos WHERE views_count > 100000

"На сколько просмотров выросли все видео 28 ноября 2025?"
→ SELECT COALESCE(SUM(delta_views_count), 0) 
   FROM video_snapshots 
   WHERE DATE(created_at) = '2025-11-28'

"Сколько видео получали новые просмотры 27 ноября 2025?"
→ SELECT COUNT(DISTINCT video_id) 
   FROM video_snapshots 
   WHERE DATE(created_at) = '2025-11-27' 
   AND delta_views_count > 0

"Сколько видео у креатора с id abc123?"
→ SELECT COUNT(*) FROM videos WHERE creator_id = 'abc123'

```

##  Управление

### Основные команды

```bash
# Запустить всё
docker-compose up -d

# Остановить
docker-compose down

# Посмотреть логи
docker-compose logs -f bot

# Перезапустить бота
docker-compose restart bot

# Проверить статус
docker-compose ps

```

### Отладка

```bash
# Проверить что Ollama работает
docker exec ollama ollama list

# Проверить БД
docker-compose exec postgres psql -U admin -d video_analytics

# Посмотреть логи всех сервисов
docker-compose logs

# Перезагрузить данные
docker-compose run --rm data-loader python scripts/load_data.py

```

### Ollama

```bash
# Проверить загруженные модели
docker exec ollama ollama list

# Загрузить модель вручную
docker exec ollama ollama pull sqlcoder:latest

# Проверить что Ollama отвечает
curl http://localhost:11434/api/tags

```


##  Тестирование

```bash
# Запустить тесты
docker-compose run --rm bot pytest

# Тесты с подробным выводом
docker-compose run --rm bot pytest -v
```
